import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const Wrapper = styled(Box)({
	paddingTop: 50,
	paddingBottom: 50,
	borderRadius: '20px',
	backgroundColor: '#fff',
});
